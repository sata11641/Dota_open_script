# 🎮 Dota 2 Sound Detector

Простой скрипт на Python, который автоматически запускает Dota 2, когда распознает звук игры.

## 📋 Требования

- Python 3.7+
- Микрофон (наушники с микро, встроенный микрофон или виртуальный микрофон)

## 📦 Установка зависимостей

```bash
pip install numpy scipy psutil pyaudio
```

**Важно для Windows:** Если у вас проблемы с установкой PyAudio, скачайте готовый `.whl` файл с [здесь](https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio).

## ⚙️ Настройка

Отредактируй файл `config.py`:

```python
# Путь к Steam
STEAM_PATH = r"C:\Program Files (x86)\Steam"

# Порог чувствительности (0-1, меньше = чувствительнее)
THRESHOLD = 0.08
```

## 🚀 Использование

1. **Создай эталон звука:**
   ```bash
   python check_audio.py
   ```
   - Включи звук Dota 2 рядом с микрофоном
   - Скрипт создаст файл `etalon.json`

2. **Запусти детектор:**
   ```bash
   python main.py
   ```
   - Программа слушает микрофон
   - При обнаружении звука запускает Dota 2
   - Стоп: `Ctrl+C`

## 📊 Что происходит

```
Детектор звука Dota 2 (PyAudio)
============================================================
Слушаем микрофон...

НАЙДЕНО! Корреляция: 0.125
Dota 2 запущена!
```

## 🎙️ Список микрофонов

Если хочешь увидеть все доступные микрофоны, добавь в `main.py`:

```python
from config import *
p = pyaudio.PyAudio()
for i in range(p.get_device_count()):
    info = p.get_device_info_by_index(i)
    print(f"ID {i}: {info['name']}")
```

## 🔧 Параметры

| Параметр | Значение | Описание |
|----------|----------|---------|
| `SAMPLE_RATE` | 44100 | Частота дискретизации (Hz) |
| `CHUNK_SIZE` | 2048 | Размер буфера (сэмплов) |
| `THRESHOLD` | 0.08 | Порог обнаружения звука |

## ⚠️ Проблемы

**Не обнаруживает звук?**
- Снизь `THRESHOLD` в `config.py` (попробуй 0.05)
- Пересоздай эталон: `python check_audio.py`
- Проверь громкость микрофона

**Dota 2 не запускается?**
- Проверь путь в `config.py`
- Убедись, что Steam установлен

## 📁 Структура проекта

```
Dota_open_script/
├── main.py              # Главный скрипт детектора
├── check_audio.py       # Создание эталона звука
├── check_process.py     # Проверка процессов
├── config.py            # Настройки (не выкладывать!)
├── etalon.json          # Эталон звука (не выкладывать!)
├── .gitignore           # Игнорируемые файлы
└── README.md            # Этот файл
```

## 📝 Лицензия

MIT

---

**Автор:** sata11641 
**Дата:** 2026
